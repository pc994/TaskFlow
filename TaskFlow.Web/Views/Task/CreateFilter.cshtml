@* @model TaskFlow.Application.ViewModels.Task.TaskFiltersVm

<h4>TaskFiltersVm</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateFilter">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Id" class="control-label"></label>
                <input asp-for="Id" class="form-control" />
                <span asp-validation-for="Id" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="IsPublic" /> @Html.DisplayNameFor(model => model.IsPublic)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
            <label>Project</label>
            <select multiple class="filter-list" name="taskFiltersVm.ProjectId" asp-for="ProjectId" id="taskFiltersVm.ProjectId">
                @foreach (var project in Model.FilterProjects)
                {
                    <option value="@project.Id">@project.Name</option>
                }
            </select>
            <label>Category</label>
            <select multiple name="taskFiltersVm.CategoriesId" asp-for="FilterCategories" id="taskFiltersVm.CategoriesId">
                @foreach (var categories in Model.FilterCategories)
                {
                    <option value="@categories.Id">@categories.Name</option>
                }
            </select>

            <label>Status</label>
            <select multiple class="filter-list" name="taskFiltersVm.StatusesId" asp-for="StatusesId" id="taskFiltersVm.StatusesId">
                @foreach (var statuses in Model.FilterStatuses)
                {
                    <option value="@statuses.Id">@statuses.Name</option>
                }
            </select>


            <label>Priority</label>
            <select multiple name="taskFiltersVm.PrioritiesId" asp-for="PrioritiesId" id="taskFiltersVm.PrioritiesId">
                @foreach (var priorities in Model.FilterPriorities)
                {
                    <option value="@priorities.Id">@priorities.Name</option>
                }
            </select>

        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

 *@